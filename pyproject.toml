[project]
name = "token-widget"
version = "0.1.0"
description = "Desktop widget showing remaining quota for LLM API accounts (Bailian Token Plan, OpenAI, OpenCode Go)"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27",
]

[project.scripts]
probe = "scripts.probe_api:main"
